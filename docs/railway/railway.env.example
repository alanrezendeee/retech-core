# Variáveis de Ambiente para Railway - Retech Core API
# Configure estas variáveis no Railway Dashboard

# ========================================
# Servidor
# ========================================
PORT=8080
ENV=production

# ========================================
# MongoDB
# ========================================
# Use a referência do serviço MongoDB do Railway:
MONGO_URI=${{MongoDB.MONGO_URL}}
MONGO_DB=retech_core

# OU se usar MongoDB Atlas:
# MONGO_URI=mongodb+srv://user:password@cluster.mongodb.net/?retryWrites=true&w=majority
# MONGO_DB=retech_core

# ========================================
# CORS
# ========================================
CORS_ENABLE=true

# Para produção, especifique os domínios permitidos:
# CORS_ORIGINS=https://seu-app.com,https://app.seu-app.com

# ========================================
# Segurança (futuro)
# ========================================
# API_KEY_REQUIRED=false

# ========================================
# Observabilidade (futuro)
# ========================================
# SENTRY_DSN=
# OTEL_ENABLED=false

# ========================================
# Notas para Railway
# ========================================
# 1. PORT: Railway injeta automaticamente, mas definimos 8080 como padrão
# 2. MONGO_URI: Use ${{MongoDB.MONGO_URL}} para referenciar o serviço MongoDB do mesmo projeto
# 3. ENV: production para Railway, development para local
# 4. CORS_ORIGINS: Configure com os domínios reais do seu frontend

# ========================================
# Como usar no Railway
# ========================================
# 1. Vá para o serviço da API no Railway
# 2. Clique na aba "Variables"
# 3. Adicione cada variável (uma por linha)
# 4. Para referenciar outro serviço: ${{NomeDoServico.VARIAVEL}}
# 5. Salve e o Railway fará re-deploy automaticamente

